version: 2
sources:
  - name: datafeed_shared_schema
    database: "{{('new_dbt_project')}}"
    schema: raw_data

    tables:
      - name: SOURCE_CUSTOMERS
      - name: SOURCE_PAYMENTS
      - name: SOURCE_ORDERS
