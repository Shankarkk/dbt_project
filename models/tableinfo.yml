version: 2
sources:
   - name: datafeed_shared_schema
     database: "{{('demo_project')}}"
     schema: raw_shema
     tables:
      - name: raw_customers
      - name: RAW_PAYMENTS
      - name: RAW_ORDERS

    
